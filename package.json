{
	"name": "back",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"migration:create": "drizzle-kit generate --dialect postgresql --schema ./src/db/schema/index.ts --out ./drizzle",
		"migration:run": "bun run --bun src/db/migrate.ts",
		"seed:run": "bun run --bun src/db/seed.ts",
		"build": "bun build --compile --outfile=todos-api src/index.ts",
		"docker:build": "docker build -t axelgalea/todos-api:latest .",
		"docker:push": "docker image push axelgalea/todos-api:latest"
	},
	"dependencies": {
		"@electric-sql/client": "^1.0.10",
		"@hono/zod-validator": "^0.7.2",
		"drizzle-orm": "^0.44.5",
		"drizzle-zod": "^0.8.3",
		"hono": "^4.9.6",
		"luxon": "^3.7.2",
		"zod": "^4.1.5"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.3",
		"@types/bun": "latest",
		"@types/luxon": "^3.7.1",
		"drizzle-kit": "^0.31.4"
	}
}
